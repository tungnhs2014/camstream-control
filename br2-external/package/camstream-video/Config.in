config BR2_PACKAGE_CAMSTREAM_VIDEO
	bool "camstream-video"
	depends on BR2_LINUX_KERNEL
	help
	  Build the CamStream synthetic V4L2 capture driver module.

	  The current driver provides fixed YUYV format negotiation, an MMAP
	  streaming lifecycle, and synthetic YUYV frame production.
